# 遗传算法调优设计说明

> 对象：`SinkhornNormalize` 算子在昇腾 910B2 上的编译期配置搜索
> 代码：`ga/`　　实验设计全文：[EXPERIMENT_PLAN.md](EXPERIMENT_PLAN.md)　　后续计划：[ROADMAP.md](ROADMAP.md)

---

## 1. 为什么是这个搜索空间

### 1.1 原方案被实测推翻的部分

最初的方案设计了一个 20 位基因空间，但那是**基于假设**的。P0/P1 阶段的真机实测推翻了其中 5 条前提：

| 原假设 | 实测结果 | 对基因空间的影响 |
|---|---|---|
| lane 利用率是瓶颈 | 10 次 Sinkhorn 迭代只占总时间 **3%** | 相关基因降级为"低影响"，但保留以供消融 |
| SoA 平面布局是最大的一步 | `DataCopyPad` 把每块补齐到 32B，SoA 会有 8 倍 lane 浪费 | **`transpose_impl` 整位删除** |
| eps 可以省掉 | 只在官方 `randn` 分布下成立，`randn×4` 就崩 | **`eps_mode` 从 `{EXACT,DROP}` 收窄为固定 `EXACT`** |
| `Reciprocal` 是 fp32 精度 | 实测只有 **9 位** | `div_mode` / `nr_steps` 成为精度关键基因 |
| tile 越小越好（MTE 分摊） | 相反，tile<64 明显更慢 | `tile_target` 的取值范围据此调整 |

**如果当初照原空间直接跑 GA，24 小时机时会花在一个「80% 的基因只影响 3% 运行时间」的空间里。** 这是调优类 GA 最典型的失败方式。

### 1.2 现在的空间

10 位基因，每一位的取值范围都有真机数据支撑：

| 基因 | 取值 | 类型 | 实测影响 |
|---|---|---|---|
| `tile_target` | 8,16,32,48,64,96,128 | 有序 | **大**：<64 时固定开销从 113us 跳到 136us |
| `eps_mode` | 0,1 | 类别 | **大**：GM 读取版比算术构造版慢 11.7us |
| `tiling_mode` | 0,1 | 类别 | **大**：每次调用同步 memcpy 慢 ~75us |
| `div_mode` | 0,1 | 类别 | 精度关键（`Reciprocal` 只有 9 位） |
| `nr_steps` | 0,1,2,3 | 有序 | 仅 `div_mode=0` 时有效 |
| `use_max` | 0,1 | 类别 | 鲁棒性关键（见 §4.2） |
| `colnorm_wide` | 0,1 | 类别 | 小：只影响那 3% 的迭代体 |
| `barrier_mode` | 0,1 | 类别 | 小：同上 |
| `copyout_mode` | 0,1 | 类别 | 未单独测过，可能影响 CopyOut 的 27us |
| `core_query` | 0,1 | 类别 | 小 |

**空间大小：笛卡尔积 7168 → 去冗余后 4480**（压缩 38%，机制见 §2.2）。

**搜索的不是源代码，是编译期开关。** 所有个体共用同一份
`op_kernel/sinkhorn_normalize_kernel_s1.asc`，区别只在 `-D` 参数：

```
基因组 t64_e1_d1n0_m1_w1_b1_o0_T1_c1
  ↓
cmake -S . -B build_ga_c07f631a3781d02d \
      -DSN_KERNEL_VARIANT=1 -DSN_TILE_TARGET=64 -DSN_S1_EPS_MODE=1 \
      -DSN_TILING_MODE=1 -DSN_S1_DIV_MODE=1 -DSN_S1_NR_STEPS=0 \
      -DSN_S1_USE_MAX=1 -DSN_S1_COLNORM_WIDE=1 -DSN_S1_BARRIER_MODE=1 \
      -DSN_S1_COPYOUT_MODE=0 -DSN_CORE_QUERY=1
```

---

## 2. 基因型（`ga/genome.py`）

### 2.1 编码

基因组是一个 `dict`，每位取自离散集合。有序基因（`tile_target`、`nr_steps`）在变异时走
±1 邻域游走（80% 概率）或均匀重采样（20%）；类别基因一律均匀重采样。

### 2.2 规范化与去重

**`repair()` 是省预算的关键。** 当 `div_mode=1`（用 `Div`）时，`nr_steps` 完全不起作用 ——
4 个不同的基因型会渲染出**完全相同的源码**。若不处理，就会白白花掉 4 倍评估预算。

```python
def repair(g):
    if g["div_mode"] == 1:
        g["nr_steps"] = 0          # 无效基因，钉死到规范值
    return g
```

缓存的键是**规范化之后**的基因组哈希，所以这类等价个体只会真正评估一次。
实测压缩效果：7168 → 4480（38%）。

### 2.3 播种

四个已知强个体作为种子，全部来自 P1 阶段的真机实测：

| 名称 | 基因组 | 实测 v1 |
|---|---|---|
| `s1d_arith` | `t64_e1_d1n0_m1_w1_b1_o0_T1_c1` | 158.55us |
| `s1c_gmeps` | `t64_e0_d1n0_m1_w1_b1_o0_T1_c1` | 164.14us |
| `s1_div` | `t64_e0_d1n0_m1_w0_b0_o0_T1_c1` | 172.69us |
| `s1b_nr` | `t64_e0_d0n2_m1_w1_b0_o0_T1_c1` | 181.74us |

其中 **`s1d_arith` 就是 `CMakeLists.txt` 当前的默认值**，即提交件实际编译出来的那一版。

---

## 3. 适应度（`ga/evaluate.py`）

### 3.1 单个体评估流程（真机约 51 秒）

```
基因组
  │
  ├─ cmake configure      ~10s   失败 → CONFIG_FAIL,    fitness = 0
  ├─ cmake --build        ~25s   失败 → COMPILE_FAIL,   fitness = 0
  ├─ check_shapes.py       ~6s   裕度≥0.5 → PRECISION_FAIL, fitness = 0
  ├─ bench_official.py    ~10s   失败 → BENCH_FAIL,     fitness = 0
  │
  └─ fitness = BASELINE_US / (v1_us + 0.5 × IQR)
```

**精度是硬门禁而非罚分项** —— 过不了直接 `fitness = 0`，速度再快也淘汰。
每个个体用独立的 `build_ga_<hash>/` 目录和独立进程，编译 300s、运行 240s 超时即杀，
评估完立即删除构建目录（60 次评估会产生数 GB 中间产物）。

### 3.2 baseline 必须钉死成常量

实测 v0（PyTorch 参考实现）在**同一台机器上跨进程波动 ±11%**（1356~1687us，n=20）。
若每次评估都重测 baseline，2% 的真实差异会被 11% 的噪声完全淹没。

因此 `BASELINE_US = 1563.0`（20 次实测的中位数）写死为常量，评估时用
`bench_official.py --baseline-ms 1.563` 跳过 v0 测量。**副作用是评估耗时直接减半。**

### 3.3 IQR 惩罚项

```
fitness = 1563 / (v1_us + 0.5 × IQR)
```

加上 `0.5 × IQR` 是为了惩罚抖动大的配置。两个中位数相同但一个稳定一个抖的配置，
应当优先选稳定的 —— 官方只测一次，抖动大的配置期望得分更低。

### 3.4 多保真

| 保真度 | 精度门禁 | 计时 | 用途 |
|---|---|---|---|
| 快档（搜索） | 2 seed | warmup 60 / repeat 180 | 排名可信，数值不可直接采信 |
| 全档（复验） | 5 seed | warmup 200 / repeat 500 | 与官方口径一致 |

快档约省 40% 单次耗时。**搜索结果必须经过全档复验才能采纳**，机制见 §6。

---

## 4. 精度门禁（`scripts/check_shapes.py`）

### 4.1 判据

12 个形状 × N 个随机种子，用官方容差 `atol=1e-2, rtol=1e-2`，指标是**裕度占用**：

```
裕度占用 = max|diff| / (atol + rtol·|ref|)
```

要求 **< 0.5**，即只用掉官方容差的一半，留一半安全边际防止过拟合到特定 seed。

### 4.2 门禁必须编码鲁棒性需求

**fitness 只优化速度，鲁棒性只能靠门禁强制。** 这不是理论担忧 ——
在 mock 上跑搜索时，top-4 候选**全部**选了 `use_max=0`（关掉 softmax 减最大值，省 2us）。

真机上这一档会被 `randn×32` 用例抓住：

| 输入 | `exp(max\|x\|)` | 结果 |
|---|---|---|
| `randn×1` | 3.3e+01 | fp32 范围内 |
| `randn×8` | 1.45e+12 | fp32 范围内 |
| **`randn×32`** | **4.38e+48** | **溢出 → NaN，门禁必挂** |

**门禁里没有的要求，等于不存在。** 覆盖的用例包括：参考形状、单矩阵、
四种尾块规模（7/65/129/333 个矩阵）、全零输入、均匀分布、`randn×8`、`randn×32`。

---

## 5. 搜索算法（`ga/search.py`）

### 5.1 岛屿模型 GA

| 参数 | 值 |
|---|---|
| 岛数 | 4（按 `tile_target` 分组） |
| 每岛种群 | 6 |
| 精英保留 | 每岛 1 |
| 选择 | 锦标赛，k=3 |
| 交叉 | 均匀交叉，`p_cx=0.7` |
| 变异 | `p_mut=0.15`/基因 |
| 迁移 | 每 5 代，环形迁移 1 个精英 |

**为什么按 `tile_target` 分岛**：实测 tile<64 存在明显性能台阶（固定开销 113us → 136us），
这是空间里最强的结构断层。若不分岛，小 tile 个体会在第一代就被全部淘汰，
该子空间从此不再被探索。分岛后各自演化，靠迁移交换有价值的基因。

岛的分组是 `tiles[i::4]`：

```
岛 0: tile ∈ {8, 64}      岛 1: tile ∈ {16, 96}
岛 2: tile ∈ {32, 128}    岛 3: tile ∈ {48}
```

**自适应机制**：连续 5 代最优无提升 → 变异率翻倍至 0.30；连续 10 代 → 重启每岛最差一半。

### 5.2 对照算法

| 算法 | 说明 |
|---|---|
| **随机搜索** | 均匀采样。**必须有** —— 没有它就无法说明 GA 的收益来自算法而非预算 |
| **局部搜索** | 逐基因扫描邻域，到局部最优后随机重启 |
| **TPE**（Optuna） | 贝叶斯优化。**强对手** —— 低维离散空间下常与 GA 相当，不做这个对照结论站不住 |

### 5.3 两个公平性设计

**① 预算按「真实评估次数」计，缓存命中不计费。**

不同算法的缓存命中率差异极大（实测 GA 63%、局部搜索 46%、随机搜索 4%）。若按总评估次数计费，
命中率高的算法会凭空获得数倍的实际探索次数。

**② 所有算法统一播种。**

最初只有 GA 和局部搜索会碰到种子，随机搜索和 TPE 从零开始 —— 于是 GA 的收敛曲线一上来就很高，
**但那是播种带来的，不是算法带来的**，这直接把「GA vs 随机」变成了「有播种 vs 无播种」。

现在所有算法在开跑前统一评估这 4 个种子（TPE 额外通过 `enqueue_trial` 让采样器从中学习），
起点完全一致。GA 剩下的优势才真正来自「从好个体繁殖」这一算法特性。
`--no-seed` 保留用于 E-C 消融（有/无播种对比）。

---

## 6. 从搜索结果到提交件（`ga/apply_best.py`）

```
ga/runs/*.sqlite          搜索结果（快档保真度）
        │
        ├─ python ga/apply_best.py            只看：列候选 → 全档复验 → 给结论
        ├─ python ga/apply_best.py --apply    确认后写入 CMakeLists.txt 默认值
        │
CMakeLists.txt 默认值      ← build.sh 不带任何 -D，用的就是这里
        │
        └─ bash submission/package.sh "队伍名" "UID"
```

**两道保险：**

1. **全档复验**：top-K 候选**和当前默认值（卫冕者）一起**用全档重测，
   各跑 3 个独立进程取中位数。搜索用的是快档，排名可信但数值不可直接采信。
2. **1.5% 噪声阈值**：v1 的测量 CV 约 2%，挑战者必须领先超过 `MARGIN=0.015` 才替换。
   **采纳一个噪声赢家，会让提交件比已经全面验证过的配置更差。**

`s1d_arith` 在整个流程里有三重身份：初始种群的种子、`CMakeLists` 的当前默认值、
以及任何挑战者必须超越的卫冕者。由于它在第一次评估就被测量并记入 `bud.best`，
**GA 报出的最优不可能差于它**。

---

## 7. 对照实验（`ga/run_all.sh`）

```bash
bash ga/run_all.sh 60     # 4 算法 × 3 seed，预算 60，约 10 小时
bash ga/run_all.sh 30     # 约 5 小时，空间只有 4480 点，30 次已能看出差异
```

`ga/analyze.py` 输出：

- **算法比较**：最优 fitness（mean±std）、最优 v1、收敛曲线 AUC
- **显著性**：Mann-Whitney U 与效应量（n=3 太小无法给可靠 p 值，报效应量本身）
- **收敛曲线**：best-so-far vs 真实评估次数，各 seed 平均
- **逐基因消融**：固定最优个体，逐位换成其它取值后的最好 fitness 与跌幅 —— **报告的核心图表**
- **健康度**：各结局分布（OK / COMPILE_FAIL / PRECISION_FAIL / …）、缓存命中率

运行期可用 `bash ga/monitor.sh` 监控（每 10 秒刷新评估数、当前最优、正在编译的目录）。

---

## 8. 局限与预期管理    

**这部分必须说清楚，否则容易对 GA 产生不切实际的期待。**

### 8.1 空间小，且起点已接近最优

有效空间只有 4480 个点，而种子 `s1d_arith` 已经是 P1 阶段手工调优的终点。
**GA 大概率只能再挤出百分之几**，而百分之几正好落在 ±2% 的测量噪声里 ——
这正是 `apply_best.py` 设置 1.5% 阈值的原因。

### 8.2 剩余性能空间本身就不大

实测的时间构成（`scripts/s1_scaling.py` 用 `t(repeat)=a+b·repeat` 拟合得出）：

| 成分 | 耗时 | 占比 | 是否在基因空间内 |
|---|---|---|---|
| launch + dispatch + sync 地板 | 62.0us | 63% | ❌ 不可控 |
| CopyIn + CopyOut | 27.0us | 27% | ⭕ 部分（`copyout_mode`） |
| 全部计算 | 10.0us | 10% | ✅ 但只值 10% |

**63% 的时间是任何自定义算子都躲不掉的 launch 开销。** 加速比的绝对上限约 14.9x，
当前 9.86x —— 剩余空间 1.5 倍，而其中大部分要靠 P4'（紧凑布局）这类结构改动，
不在当前基因空间里。

### 8.3 那 GA 的价值在哪

1. **方法学与对照实验** —— 收敛曲线、GA vs TPE vs 随机、同预算比较，这是项目的正题
2. **逐基因消融** —— 把 P0/P1 手工得到的结论系统化、可量化，是报告最有说服力的图表
3. **为后续扩展打好基座** —— 真正能扩大空间的是把紧凑布局做成**结构基因**（P4'）
   和代码级变异（P5'），届时评估基础设施可以直接复用

### 8.4 mock 后端的定位

`ga/evaluate.py` 提供一个 Mac 上可跑的代价模型，参数全部标定自真机实测
（地板 62us、CopyIn/CopyOut 27us、eps 差值 11.7us、每次迭代 0.45~0.83us 等）。

**它只能验证 GA 主循环本身，不能用来发现新配置** —— 因为它的最优点按构造就是
已知的 `s1d_arith`。已知的失真会及时修正，例如 mock 最初没有反映
`use_max=0` 在 `randn×32` 下会溢出，导致搜索被误导，发现后立即修正。

---

## 9. 文件清单

| 文件 | 作用 |
|---|---|
| `ga/genome.py` | 基因定义、`repair()` 规范化、变异/交叉、CMake 参数渲染 |
| `ga/evaluate.py` | 编译 → 精度门禁 → 计时 → fitness；含 mock 代价模型 |
| `ga/cache.py` | SQLite 去重缓存，键为规范化基因组哈希 |
| `ga/search.py` | 岛屿 GA / 随机 / 局部 / TPE，共用 `Budget` 与统一播种 |
| `ga/apply_best.py` | 全档复验 + 噪声阈值仲裁 + 写回 `CMakeLists.txt` |
| `ga/analyze.py` | 收敛曲线、显著性、逐基因消融 |
| `ga/run_all.sh` | 4 算法 × 3 seed 对照矩阵 |
| `ga/monitor.sh` | 运行期监控 |
