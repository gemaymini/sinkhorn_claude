现在两个算法的前 4 次评估完全相同（就是 4 个种子），起点一致。下面回答你的问题。

## GA 在搜什么

**不是源码，是 12 个编译开关。** 所有个体用的都是同一份 `sinkhorn_normalize_kernel_s1.asc`，区别只在 `-D` 参数：

```
基因组                    →  cmake -S . -B <dir> -DSN_KERNEL_VARIANT=1
t64_e1_d1n0_m1_w1_b1_o0_T1_c1                    -DSN_TILE_TARGET=64
                                                 -DSN_S1_EPS_MODE=1
                                                 -DSN_S1_DIV_MODE=1  ...
```

10 位基因，去掉冗余组合后 **4480 个有效点**。

## s1d_arith 有三重身份

它就是空间里的一个坐标：`t64_e1_d1n0_m1_w1_b1_o0_T1_c1`（哈希 `c07f631a3781d02d`）。

**① 种子个体** —— 初始种群里直接放进去。岛屿按 `tile_target` 分，四个种子的 tile 都是 64，所以都落在岛 0：

```
岛 0: tile ∈ [8, 64]    播种 s1d_arith, s1c_gmeps, s1_div
岛 1: tile ∈ [16, 96]   全随机
岛 2: tile ∈ [32, 128]  全随机
岛 3: tile ∈ [48]       全随机
```

这带来一个重要保证：**GA 报出的最优不可能差于 s1d_arith**，因为它在第 1 次评估就被测了，`bud.best` 会记住。

**② CMakeLists 的当前默认值** —— `build.sh` 不带 `-D`，编出来的就是它。

**③ 卫冕者** —— `apply_best.py` 里任何挑战者必须领先它 >1.5% 才会被采纳。

## 单个个体的评估流程（~51s）

```
基因组
  ↓ cmake -S . -B build_ga_<hash> -D...        ~10s   失败 → CONFIG_FAIL, fitness=0
  ↓ cmake --build --target ...                 ~25s   失败 → COMPILE_FAIL, fitness=0
  ↓ check_shapes.py --seeds 2                  ~6s    裕度≥0.5 → PRECISION_FAIL, fitness=0
  ↓ bench_official.py --baseline-ms 1.563      ~10s
  ↓ fitness = 1563 / (v1_us + 0.5×IQR)
  ↓ 写入 sqlite（键 = 规范化基因组哈希，下次命中直接返回，不计预算）
  ↓ rm -rf build_ga_<hash>
```

精度是**硬门禁**，不是罚分 —— 过不了直接 fitness=0，速度再快也淘汰。

## 岛屿 GA 主循环

```
每一代：
  1. 评估四个岛的全部个体（6 个/岛）
  2. 每岛内：精英保留 1 个 → 锦标赛(k=3)选亲 → 均匀交叉(p=0.7) → 变异(p=0.15)
  3. 每 5 代：岛间迁移 1 个精英（环形）
  4. 连续 5 代无提升 → 变异率翻倍；连续 10 代 → 重启每岛最差一半
直到预算 60 次真实评估耗尽
```

按 `tile_target` 分岛的理由：实测 tile<64 有明显性能台阶，是空间里最强的结构断层，分岛能防止小 tile 个体在早期被直接淘汰而丢失多样性。

## 跑完怎么回到提交件

```
ga/runs/*.sqlite → apply_best.py → 全档复验 top-K + 卫冕者
                                 → 领先 >1.5% 才写 CMakeLists 默认值
                                 → package.sh
```

---

## 解释的时候发现一个会让对照实验失效的问题

原来只有 `ga` 和 `local` 会碰到种子，`random` 和 `tpe` **从零开始**。于是 GA 的收敛曲线一上来就是 9.86 —— **那是播种给的，不是算法给的**，直接把"GA vs 随机"这个核心对照变成了"有播种 vs 无播种"。

已修：**所有算法统一先评估这 4 个种子**（上面 mock 输出里两个算法的前 4 次已经完全一样）。这样起点一致，GA 剩下的优势才真正来自"从好个体繁殖"这一算法特性。TPE 也通过 `enqueue_trial` 让采样器学到种子，不然它被不公平地削弱。`--no-seed` 保留下来做 E-C 消融（有/无播种）。

**对你正在跑的这一轮的影响**：`ga_s0` 是用旧代码跑的，和后续用新代码跑的 `random`/`tpe` 不可比。建议这一轮跑完后：

```bash
rm -rf ga/runs && bash ga/run_all.sh 30
```

预算降到 30（每轮约 26 分钟，12 轮约 5 小时）。空间才 4480 点，30 次评估足够看出算法差异，而且省一半机时。