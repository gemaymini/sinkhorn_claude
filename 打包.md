拿到真正的源码了。逐条核对下来有**一个重要的好消息**和几处需要改的地方：

核对完官方源码，**有一个关键发现**：`load_ks_module` 里 `module.__file__ = str(path)` 是**明确设置的**，所以提交文件能拿到自己的路径 —— 把 `.so` 放在 `model_new.py` 旁边就能被找到。这让提交件的自包含性有了保障。

据此改几处：

`model_ref.py` 是 v0 文件，本来就不该有 `ModelNew` —— 是我的检查脚本没区分角色。修掉，并写一个**完整的合规自检**：

现在写完整的合规自检 —— 逐条对照官方规则 4.2 / 4.3 / 5.1 / 5.2 和 `auto_bench.py` 的实际行为：

两个"不符"都是**在源码目录 `submission/` 里跑**造成的，不是真问题：Mac 上没有 `torch_npu`；`op_kernel` 等在仓库根目录，只有打包时才会被复制进来。修掉这两个环境假设：

29 项检查全过（唯一的"注意"是 `results/` 还空着，跑完真机测试就有了）。把新文件接进打包流程，并让合规检查成为**打包的最后一道闸**：

拿到官方源码后逐条核对了一遍，有**一个关键发现**和**四处需要改的地方**。

## 关键发现：`__file__` 是有的

```python
module = types.ModuleType(module_name)
module.__file__ = str(path)          # ← 官方明确设置
```

我之前按"最坏情况"假设 `__file__` 可能不存在，做了防御性处理。现在确认它一定有，**所以把 `.so` 放在 `model_new.py` 同目录就能被自动找到，不需要任何环境变量**。查找顺序已调整为本文件目录优先，提交包也按这个组织。

另外 `sys.path.insert(0, str(path.parent))` 只在 `exec` 期间有效，之后会还原 —— 所以不能指望它，`__file__` 才是可靠路径。

## 四处改动

**1. `_is_safe_literal` 换成官方原版。** 我之前用 `ast.literal_eval` 近似，官方实际支持 `Constant` / `Tuple,List,Set` / `Dict` / 一元 `+-`。现在逐字照搬，`model_ref.py` 里的 `n0 = 1` 这类赋值确认能存活。

**2. 补了 `model_ref.py`。** 官方是 `--v0_file` 和 `--v1_file` **两个独立文件**：v0 要 `Model`，v1 要 `ModelNew`。之前只有一个文件，评委没法直接跑。现在逐字照抄了 Task03 参考实现，`run_auto_bench.sh` 可以一条命令跑官方原版评测。

**3. `forward` 加了连续性保护。** kernel 假定输入连续，官方 harness 经过 `clone_value` + `_move_to_device` 传进来的确实是连续的，但非连续输入会算出错误结果 = 直接 0 分。`is_contiguous()` 是一次极廉价的 C 调用，这个保险值得付。

**4. 新增 `check_compliance.py` —— 29 项合规自检。** 这是你要的"再次检查是否按要求"：

| 类别 | 代表性检查 |
|---|---|
| 规则 5.2 | UTF-8 编码、Python ≥ 3.10 |
| auto_bench 兼容 | AST 过滤后可 exec、必需符号齐全、**顶层无被丢弃的可执行语句**、`get_inputs` 返回类型、**`ModelNew` 的 `__init__`/`forward` 参数与 `Model` 逐个比对** |
| 规则 5.1 反作弊 | **`forward` 的每一条返回路径都调用自定义算子**（用 AST 遍历所有 `Return` 节点验证）、不含 `softmax` 等内置实现痕迹、**无异常捕获** |
| 规则 4.2 | 算子代码 / README / 环境配置 / 运行脚本 / 性能结果齐备 |
| 规则 4.3 | README 四个必备章节 |
| 运行时 | 算子注册成功、**调用计数器递增确认真实进入 kernel**、官方容差下结果一致 |

规则 5.1 那条特意做成"检查所有 return 路径"而不是"检查有没有调用" —— 因为我的 `forward` 里有一个 `if x.device.type == "npu"` 的分支，规则明令禁止"用条件分支规避自定义算子执行"。这个检查能证明**两条分支都走自定义算子**，不是 fallback。

**当前状态：29 项检查 28 通过、1 注意（`results/` 还空着，跑完真机测试就有）、0 不符。**

## 打包流程

合规检查现在是**打包的最后一道闸**，不通过直接中止：

```bash
source /usr/local/Ascend/ascend-toolkit/latest/set_env.sh
bash submission/package.sh "你的队伍名" "你的UID"
```

流程：收集源码 → 在包内独立编译（验证自包含）→ 精度门禁 → 官方口径评测 → 迭代扫描 → **29 项合规自检** → 打 tar.gz，最后打印邮件主题、收件邮箱、PR 标题。

跑完后建议再用官方原版复核一次：

```bash
cd dist/【2026KernelSwift算子创新大赛】-*/ && bash run_auto_bench.sh
```

这一步跑的是从 GitHub 下载的官方 `auto_bench.py`，不是我们复刻的 `bench_official.py`。两者应该给出一致的加速比 —— **如果不一致，以官方的为准**，并且说明我复刻的口径有偏差，需要回头修 GA 的 fitness。s


好问题 —— **不行，现在还缺一环。**

`package.sh` 走的是 `build.sh`，而 `build.sh` 不带任何 `-D`，用的是 `CMakeLists.txt` 里的默认值（我刚对齐成 `s1d_arith`）。GA 的结果躺在 `ga/runs/*.sqlite` 里，**两者之间没有任何连接**。

而且还有个更微妙的问题：GA 搜索用的是**快档**（2 seed、warmup 60/repeat 180），搜出来的最优不能直接采信 —— 必须用全档重新验证，否则可能采纳一个噪声赢家。补上这一环：

两个问题，都值得修：

你问到点子上了 —— **原来确实缺一环，现在补上了。**

## 完整链路

```
ga/runs/*.sqlite          GA 搜索结果（快档保真度）
        │
        ├─ python ga/apply_best.py           只看：列候选 + 全档复验 + 给结论
        ├─ python ga/apply_best.py --apply   确认后写入 CMakeLists.txt 默认值
        │
CMakeLists.txt 默认值      ← build.sh 不带任何 -D，用的就是这里
        │
        └─ bash submission/package.sh "队伍名" "UID"
```

所以 GA 跑完后是**两条命令**：

```bash
python3 ga/apply_best.py --apply
bash submission/package.sh "你的队伍名" "你的UID"
```

## `apply_best.py` 的两道保险

**保险一：全档复验。** GA 搜索用的是快档（精度 2 seed、warmup 60/repeat 180），排名可信但数值不可直接采信。脚本会把 top-K 候选**和当前默认值（卫冕者）一起**用全档重测（精度 5 seed + warmup 200/repeat 500，各跑 3 个独立进程取中位数）。

**保险二：1.5% 噪声阈值。** v1 的测量 CV 约 2%，挑战者必须领先超过阈值才替换。否则会打印：

> 未超过噪声阈值 1.5%（v1 的测量 CV 约 2%）—— 采纳噪声赢家会让提交件比已验证的配置更差，保持当前默认值。

这条很重要：GA 大概率只能挤出百分之几，而百分之几正好在噪声里。宁可保留已经全面验证过的配置。

另外 `package.sh` 现在会在打包前**打印生效的 12 个开关**并存进 `results/build_config.txt`，不会再出现"不知道包里是哪个配置"的情况。

## 顺带发现并修了一个 mock 的失真

用 mock 数据演练 `apply_best` 时，top-4 候选**全是 `use_max=0`** —— GA 发现关掉减 max 能省 2us。这正是我之前担心的"fitness 只优化速度"。

查了一下真机会不会放行：

| 输入 | `exp(max\|x\|)` | 结果 |
|---|---|---|
| randn×1 | 3.3e+01 | fp32 范围内 |
| randn×8 | 1.45e+12 | fp32 范围内 |
| **randn×32** | **4.38e+48** | **溢出 → NaN，门禁必挂** |

**上一轮加的 `randn×32` 用例正好能抓住它** —— 这就是当时加它的原因，现在验证了确实有牙齿。

但 mock 的代价模型不知道这件事，会误导在 Mac 上做的 GA 调参。已经修正：mock 里 `use_max=0` 现在直接判精度失败。修完重跑，候选里 `m0` 全部消失。

---

现在整条链路是通的。GA 跑完直接：

```bash
python3 ga/apply_best.py              # 先看结论，不改任何东西
python3 ga/apply_best.py --apply      # 确认后再写
bash submission/package.sh "队伍名" "UID"
```

如果 GA 没跑，`apply_best.py` 会直接告诉你"用当前默认值打包即可"，不会报错。